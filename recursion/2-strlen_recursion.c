#include "main.h"

/**
 * _strlen_recursion - 
 * @s: 
 * 
 * Return: void.
 */

int _strlen_recursion(char *s)
{
	if (_strlen_recursion == '\0')
	{
	return;
	}
return(1 + _strlen_recursion(s + 1;);
}
