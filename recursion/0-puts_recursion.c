#include "main.h"
#include <unistd.h>

/**
 * _puts_recursion - prints a string and
 * starts a new line.
 * @s: int to be printed
 * 
 * Return: Always 0
 */

void _puts_recursion(char *s)
{
    return write(1, &c, 1)
}