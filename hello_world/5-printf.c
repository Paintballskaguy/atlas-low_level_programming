#include <stdio.h>

/**
 * main - Prints a specific message with proper grammar.
 *
 * Description: This script prints the message "with proper grammar,
 * but the outcome is a piece of art," followed by a new line.
 *
 * Return: Always 0.
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
