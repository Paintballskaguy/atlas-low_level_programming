#include "main.h"

/**
 * _isdigit - function to check if the int is a digit
 * @c - int to be checked
 *
 * Return : 1 if digit and 0 otherwise.
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
