#include <stdio.h>
/* main this code will print out the alaphabit in lowercase */
/* return should be 0*/

int main(void)
{
	char A;
	for (A = 'a'; A <= 'z'; A++)
{
	putchar(A);
}
	putchar('\n');
	return (0);
	}
