#include <stdio.h>
/*
 * main: entry point is printing alphabet in lowercase.
 * Return: return should be 0 (success)
 */
int main (void)
{
	char A;
	{
	while (A = 'a'; A <= 'z'; A++);
	
	putchar (A);

	putchar ('\n');

		return (0);
}

